# asyncpg — асинхронный драйвер PostgreSQL
asyncpg==0.31.0

# cyrtranslit — транслитерация кириллицы в латиницу
cyrtranslit==1.2.0

# google-api-python-client — клиент для Google APIs (в т.ч. Calendar)
google-api-python-client==2.169.0

# google-auth — базовая аутентификация Google (Credentials, Request)
google-auth==2.48.0

# google-auth-oauthlib — OAuth 2.0 flow для Google
google-auth-oauthlib==1.2.0

# python-dotenv — загрузка конфигурации из .env
python-dotenv==1.2.0

# python-telegram-bot — Telegram Bot API (v22, async)
python-telegram-bot==22.6

vkbottle>=4.7.0
loguru>=0.7.0
aiohttp>=3.8.0